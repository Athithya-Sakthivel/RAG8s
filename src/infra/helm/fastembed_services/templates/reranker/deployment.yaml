{{- if .Values.reranker.enabled }}
{{- $service := "reranker" }}
{{- $component := "reranker" }}
{{- $name := "reranker" }}
{{- $labels := dict "name" $name "component" $component "instance" $name }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "fastembed.deploymentName" (dict "service" $service) }}
  namespace: {{ .Values.reranker.namespace }}
  labels:
    {{- include "fastembed.labels" (dict "Chart" .Chart "Release" .Release "name" $name "component" $component "instance" $name) | nindent 4 }}
spec:
  {{- if not .Values.reranker.hpa.enabled }}
  replicas: {{ .Values.reranker.replicas }}
  {{- else }}
  replicas: {{ .Values.reranker.hpa.min }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "fastembed.selectorLabels" $labels | nindent 6 }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 25%
      maxSurge: 25%
  template:
    metadata:
      labels:
        {{- include "fastembed.podLabels" $labels | nindent 8 }}
    spec:
      serviceAccountName: {{ include "fastembed.serviceAccountName" (dict "service" $service) }}
      nodeSelector:
        {{- toYaml .Values.global.nodeSelector | nindent 8 }}
      tolerations:
        {{- toYaml .Values.global.tolerations | nindent 8 }}
      securityContext:
        fsGroup: 1000
        runAsNonRoot: true
      containers:
      - name: {{ $name }}
        image: {{ .Values.reranker.embedderImage }}
        ports:
        - containerPort: 8202
        env:
        - name: ENV
          value: {{ .Values.global.logLevel | upper | quote }}
        - name: RERANKER_HOST
          value: "0.0.0.0"
        - name: RERANKER_PORT
          value: "8202"
        - name: RERANKER_LOGLEVEL
          value: {{ .Values.global.logLevel | upper | quote }}
        - name: RERANKER_MODEL_NAME
          value: {{ .Values.reranker.modelName | quote }}
        - name: RERANKER_MAX_DOCS
          value: {{ .Values.reranker.batchSize | quote }}
        - name: RERANKER_CUDA
          value: {{ if and .Values.global.cuda (gt (int .Values.reranker.gpuCount) 0) }}"1"{{ else }}"0"{{ end }}
        - name: PRELOAD_MODEL
          value: {{ if .Values.global.cuda }}"1"{{ else }}"0"{{ end }}
        livenessProbe:
          httpGet:
            path: /health
            port: 8202
          initialDelaySeconds: 30
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 6
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8202
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        startupProbe:
          httpGet:
            path: /readyz
            port: 8202
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 60
        resources:
          {{- toYaml .Values.reranker.hpa.resources | nindent 10 }}
        securityContext:
          runAsNonRoot: true
          runAsUser: 1000
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
        volumeMounts:
        - name: tmp-writable
          mountPath: /tmp
        - name: tmp-writable
          mountPath: /var/tmp
        - name: tmp-writable
          mountPath: /usr/tmp
        - name: models-cache
          mountPath: /models_cache
      volumes:
      - name: tmp-writable
        emptyDir: {}
      - name: models-cache
        emptyDir: {}
{{- end }}