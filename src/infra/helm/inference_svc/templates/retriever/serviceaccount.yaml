{{- if .Values.retriever.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.retriever.serviceAccount.name }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "inference-services.selectorLabels" (dict "component" "retriever") | nindent 4 }}
    {{- include "inference-services.commonLabels" . | nindent 4 }}
  {{- with .Values.retriever.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}