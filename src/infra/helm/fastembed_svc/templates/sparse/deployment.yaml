{{- if .Values.sparse.enabled }}
{{- $service := "sparse" }}
{{- $component := "embedder" }}
{{- $name := "sparse" }}
{{- $labels := dict "name" $name "component" $component "instance" $name }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "fastembed.deploymentName" (dict "service" $service) }}
  namespace: {{ .Values.sparse.namespace }}
  labels:
    {{- include "fastembed.labels" (dict "Chart" .Chart "Release" .Release "name" $name "component" $component "instance" $name) | nindent 4 }}
spec:
  {{- if not .Values.sparse.hpa.enabled }}
  replicas: {{ .Values.sparse.replicas }}
  {{- else }}
  replicas: {{ .Values.sparse.hpa.min }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "fastembed.selectorLabels" $labels | nindent 6 }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 25%
      maxSurge: 25%
  template:
    metadata:
      labels:
        {{- include "fastembed.podLabels" $labels | nindent 8 }}
    spec:
      serviceAccountName: {{ include "fastembed.serviceAccountName" (dict "service" $service) }}
      nodeSelector:
        {{- toYaml .Values.global.nodeSelector | nindent 8 }}
      tolerations:
        {{- toYaml .Values.global.tolerations | nindent 8 }}
      securityContext:
        fsGroup: 1000
        runAsNonRoot: true
      containers:
      - name: {{ $name }}
        image: {{ .Values.sparse.embedderImage }}
        ports:
        - containerPort: 8201
        env:
        - name: ENV
          value: {{ .Values.global.logLevel | upper | quote }}
        - name: SPARSE_HOST
          value: "0.0.0.0"
        - name: SPARSE_PORT
          value: "8201"
        - name: SPARSE_LOGLEVEL
          value: {{ .Values.global.logLevel | upper | quote }}
        - name: SPARSE_MODEL_NAME
          value: {{ .Values.sparse.modelName | quote }}
        - name: SPARSE_BATCH_SIZE
          value: {{ .Values.sparse.batchSize | quote }}
        - name: SPARSE_CUDA
          value: {{ if and .Values.global.cuda (gt (int .Values.sparse.gpuCount) 0) }}"1"{{ else }}"0"{{ end }}
        - name: PRELOAD_MODEL
          value: "1"
        - name: FASTEMBED_CACHE
          value: /models_cache
        - name: HF_HOME
          value: /models_cache
        - name: HUGGINGFACE_HUB_CACHE
          value: /models_cache
        - name: TRANSFORMERS_CACHE
          value: /models_cache
        livenessProbe:
          httpGet:
            path: /health
            port: 8201
          initialDelaySeconds: 30
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 6
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8201
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        startupProbe:
          httpGet:
            path: /readyz
            port: 8201
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 60
        resources:
          {{- toYaml .Values.sparse.hpa.resources | nindent 10 }}
        securityContext:
          runAsNonRoot: true
          runAsUser: 1000
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
        volumeMounts:
        - name: tmp-writable
          mountPath: /tmp
        - name: tmp-writable
          mountPath: /var/tmp
        - name: tmp-writable
          mountPath: /usr/tmp
        - name: models-cache
          mountPath: /models_cache
      volumes:
      - name: tmp-writable
        emptyDir: {}
      - name: models-cache
        emptyDir: {}
{{- end }}