apiVersion: v1
kind: Service
metadata:
  name: cloudflared-metrics
  namespace: inference
  labels:
    app.kubernetes.io/name: cloudflared
    app.kubernetes.io/component: tunnel
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: cloudflared
    app.kubernetes.io/component: tunnel
  ports:
  - name: metrics
    port: 2000
    targetPort: 2000
    protocol: TCP
