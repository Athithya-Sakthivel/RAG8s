name: reranker-image-ci

on:
  push:
    branches: [ main ]
    paths:
      - src/services/reranker/**
      - .github/workflows/reranker.yaml
  pull_request:
    branches: [ main ]
    paths:
      - src/services/reranker/**
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

concurrency:
  group: reranker-image-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  AWS_REGION: ap-south-1
  AWS_ACCOUNT_ID: "681802563986"
  ROLE_NAME: gh-actions-reranker
  ECR_REPOSITORY: reranker
  IMAGE_NAME: reranker

jobs:
  build-test-scan-push:
    name: Build, test, scan, publish reranker image
    runs-on: ubuntu-24.04
    timeout-minutes: 40

    steps:
      - name: Checkout
        uses: actions/checkout@v5.0.0
        with:
          fetch-depth: 1

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4.0.0

      - name: Construct AWS role ARN
        id: aws-role
        run: |
          echo "arn=arn:aws:iam::${{ env.AWS_ACCOUNT_ID }}:role/${{ env.ROLE_NAME }}" >> "$GITHUB_OUTPUT"

      - name: Compute image metadata
        id: image-meta
        shell: bash
        run: |
          set -euo pipefail
          export TF_VAR_region="ap-south-1"
          IMAGE_TAG="${IMAGE_TAG:-$(TZ=Asia/Kolkata date +%Y-%m-%d-%H-%M)--$(git rev-parse --short HEAD)}"
          REGISTRY_HOST="${{ env.AWS_ACCOUNT_ID }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com"
          PRIMARY_IMAGE="${REGISTRY_HOST}/${{ env.ECR_REPOSITORY }}:${IMAGE_TAG}"
          STAGING_IMAGE="${REGISTRY_HOST}/${{ env.ECR_REPOSITORY }}:staging"
          echo "image_tag=${IMAGE_TAG}" >> "$GITHUB_OUTPUT"
          echo "repo_name=${{ env.ECR_REPOSITORY }}" >> "$GITHUB_OUTPUT"
          echo "primary_image=${PRIMARY_IMAGE}" >> "$GITHUB_OUTPUT"
          echo "staging_image=${STAGING_IMAGE}" >> "$GITHUB_OUTPUT"

      - name: Build image
        id: build-image
        uses: docker/build-push-action@v7.1.0
        with:
          context: src/services/reranker
          file: src/services/reranker/Dockerfile
          load: true
          push: false
          pull: true
          tags: |
            ${{ steps.image-meta.outputs.primary_image }}
            ${{ steps.image-meta.outputs.staging_image }}
          cache-from: type=gha,scope=reranker
          cache-to: type=gha,mode=max,scope=reranker

      - name: Run service tests
        shell: bash
        run: |
          set -euo pipefail
          export TF_VAR_region="ap-south-1"
          cd src/services/reranker
          export RERANKER_MODEL_NAME="${RERANKER_MODEL_NAME:-Xenova/ms-marco-MiniLM-L-6-v2}"
          export RERANKER_MAX_DOCS="${RERANKER_MAX_DOCS:-50}"
          export RERANKER_IMAGE_TAG="${IMAGE_TAG:-${{ steps.image-meta.outputs.image_tag }}}"
          export IMAGE_REPO="${IMAGE_REPO:-${{ steps.image-meta.outputs.repo_name }}}"
          chmod +x test_reranker.sh
          TEST_MODE=cpu \
          RERANKER_MODEL_NAME="${RERANKER_MODEL_NAME}" \
          RERANKER_MAX_DOCS="${RERANKER_MAX_DOCS}" \
          ./test_reranker.sh

      - name: Scan built image with Trivy
        uses: aquasecurity/trivy-action@v0.35.0
        with:
          image-ref: ${{ steps.build-image.outputs.imageid }}
          format: table
          scanners: vuln
          severity: CRITICAL
          ignore-unfixed: true
          exit-code: 1

      - name: Configure AWS credentials for ECR
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: aws-actions/configure-aws-credentials@v6.1.1
        with:
          role-to-assume: ${{ steps.aws-role.outputs.arn }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Log in to ECR
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: aws-actions/amazon-ecr-login@v2.1.5

      - name: Delete existing staging tag to allow overwrite
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        shell: bash
        run: |
          aws ecr batch-delete-image \
            --repository-name "${{ env.ECR_REPOSITORY }}" \
            --image-ids imageTag=staging \
            --region "${{ env.AWS_REGION }}" || true

      - name: Push image tags
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        shell: bash
        run: |
          set -euo pipefail
          export TF_VAR_region="ap-south-1"
          docker push "${{ steps.image-meta.outputs.primary_image }}"
          docker push "${{ steps.image-meta.outputs.staging_image }}"